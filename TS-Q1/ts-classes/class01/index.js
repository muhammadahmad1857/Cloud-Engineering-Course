"use strict";
console.log("hello world");
// First we run command "tsc --init" to initialize the Typescript environment
// Then we run command "npm init"  to create  the npm package
// and then we run command "tsc 'fileName.ts' " to make a auto javascript fileor to compile it in javascript.
// Then we run command "node filename.js" to run the code and see the output.
// We need to run this command whenever we runcode.
// for resisting this.
// we use "tsc 'filename.ts' -w" or "tsc -w" to compile the code when the code changes.
// and then we use "nodemon node filename.js" to run the code when the code changes
